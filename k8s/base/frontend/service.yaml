apiVersion: v1
kind: Service
metadata:
  name: front-service
spec:
  selector:
    app: js-front
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer # Para acceder desde tu navegador vía Minikube